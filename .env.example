# Copie para `.env` (chmod 600). Os valores abaixo são os de uma VPS padrão:
# clone em /root/projetos/inemaccbot, rodando como root. Em outra máquina/usuário,
# troque /root pelo home real — o parser NÃO expande ~ nem $HOME.
# O parser é minimalista: CHAVE=valor, e `#` só comenta em LINHA PRÓPRIA —
# comentário no fim da linha vira parte do valor.

# --- obrigatórias: sem uma delas o boot falha alto, antes de subir worker ---
BOT_TOKEN=
# Não sabe o id do seu chat? Deixe 0: é o modo pareamento, e o PRIMEIRO /ping
# que o bot receber cadastra aquele chat como dono e reescreve esta linha.
# Vazio NÃO é pareamento — vazio derruba o boot, de propósito.
ALLOWED_CHAT_IDS=0
QUEUE_DB=/root/projetos/inemaccbot/inemaccbot.db
STATE_DIR=/root/projetos/inemaccbot/state
LOG_FILE=/root/projetos/inemaccbot/inemaccbot.log

# --- opcionais: o default vale se a linha ficar de fora ---
# Onde moram os repos de domínio (promoavatar, promoavatar3).
# default: $HOME/projetos
PROJETOS_DIR=/root/projetos
# Pasta servida por HTTP e as bases de URL do link final (duas por causa da rede dupla).
# default: $HOME/projetos/output/reels
PUBLICO_DIR=/root/projetos/output/reels
# uma ou mais bases, separadas por vírgula
PUBLICO_URLS=http://SEU.IP.DA.VPS:8202

# Caminho ABSOLUTO do binário do Claude Code — nunca a string `claude`: sob
# systemd o PATH é mínimo e cairia num binário velho (ou em nenhum). O boot
# recusa subir se o caminho não existir. Descubra o seu com:  which claude
# (npm global costuma dar /usr/local/bin/claude ou /usr/bin/claude; o instalador
# nativo dá $HOME/.local/bin/claude, que é o default quando a linha fica fora.)
# default: $HOME/.local/bin/claude
CLAUDE_BIN=/usr/local/bin/claude

# Perfil de execução dos jobs de agente.
MOTOR_PADRAO=claude
MODELO_PADRAO=sonnet
ESFORCO_PADRAO=low

# HeyGen. As três linhas abaixo são CAMINHO, não segredo.
# default: heygen (precisa estar no PATH)
HEYGEN_CLI=heygen
# default: $HOME/projetos/openpcbotv2/.env
HEYGEN_ENV_PATH=/root/projetos/openpcbotv2/.env
# default: $HOME/.cache/inemaccbot/perfil-heygen
HEYGEN_PERFIL_CHROME=/root/.cache/inemaccbot/perfil-heygen

# A API key do HeyGen (rotas `| api` e `| creditos`) NÃO é lida deste arquivo por
# padrão: ela é lida, em runtime, do arquivo que o HEYGEN_ENV_PATH acima aponta —
# é assim que o segredo de terceiro fica no arquivo do dono dele, e não copiado
# por aqui. O código aceita QUALQUER arquivo com uma linha HEYGEN_API_KEY=.
#
# Numa máquina que não tem o openpcbotv2 (uma VPS, por exemplo), há dois caminhos:
#
#   1. arquivo só dela  — o recomendado, mantém segredo fora do .env do bot:
#        echo 'HEYGEN_API_KEY=hg_...' > ~/.config/inemaccbot/heygen.env
#        chmod 600 ~/.config/inemaccbot/heygen.env
#        HEYGEN_ENV_PATH=/root/.config/inemaccbot/heygen.env
#
#   2. aqui mesmo — aponte o HEYGEN_ENV_PATH para ESTE arquivo e descomente a
#      linha abaixo. Funciona sem mudar código, mas junta segredo e config num
#      arquivo só; se fizer isso, garanta o chmod 600 e nunca versione o .env.
#        HEYGEN_ENV_PATH=/root/projetos/inemaccbot/.env
# HEYGEN_API_KEY=

# --- repassadas aos SCRIPTS dos repos de domínio ---
# O bot não lê nenhuma destas: ele as publica no ambiente, e quem usa são os
# scripts que as fases disparam (gen-imagem.py, transcribe-groq.sh). O default
# de cada uma é o comportamento de sempre, então numa máquina com GPU e com o
# openpcbotv2 as três linhas podem ficar de fora.

# Onde está o inemaimg (a GPU que gera as imagens do reel). Numa máquina sem
# GPU, um túnel para a de casa resolve:  ssh -R 8000:localhost:8000 <vps>
# default: http://localhost:8000
INEMAIMG_HOST=http://localhost:8000
# default: flux2-klein
INEMAIMG_MODEL=flux2-klein

# QUEM gera a imagem. `inemaimg` (default) é a GPU local. Numa máquina sem GPU,
# `agnes` é API e custa US$ 0 — mas ela NÃO tem seed, então a promessa de "mesmo
# reel, mesma imagem" cai. Se a reprodutibilidade importar mais que a
# independência, mantenha `inemaimg` e faça um túnel para a GPU de casa:
#   ssh -R 8000:localhost:8000 <vps>
# `kie` e `fal` ainda não estão implementados — o script recusa em vez de fingir.
# default: inemaimg | agnes
IMG_PROVEDOR=inemaimg
# Arquivo com a chave do provedor (ex.: AGNES_API_KEY=...), chmod 600.
# Alternativa: a própria variável no ambiente, que tem precedência.
IMG_ENV_PATH=/root/.config/inemaccbot/heygen-e-imagem.env

# Arquivo com a GROQ_API_KEY (transcrição do avatar → legenda e corte por fala).
# Alternativa: exportar GROQ_API_KEY direto no ambiente, que tem precedência.
# default: $HOME/projetos/openpcbotv2/.env
GROQ_ENV_PATH=/root/projetos/openpcbotv2/.env
