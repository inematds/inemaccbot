# Session Handoff — 2026-08-06/07: fase 2 e 3 saem do agente, e o promoavatar3 se separa

## Where it started

Começou como análise de custo por fase (A#19–A#29) e virou execução. A medição
mostrou a navegação do HeyGen com 57% do gasto e o reel com 32%; a fase de texto
era a menor. A partir daí duas fases de agente viraram código, o **promoavatar
foi congelado** por decisão do dono, e o **promoavatar3 virou autônomo**.
Restrição que atravessou tudo: *instrução em prompt não é portão* — o que vale é
script, exit code ou caminho removido.

## Applied / shipped

- [confirmed] **`reel.montar`** (`kind: function`) — fase de reel sem agente:
  dispara `montar-reel.py` destacado, **segura a vaga da fila** com
  `esperarArtefato`, entrega no canal —
  `/home/nmaldaner/projetos/inemaccbot/src/fila/tarefas/reel.ts`
- [confirmed] **Rota `| estudio`** — `heygen.estudio` + Playwright headless:
  clona o `TEMPLATE-AVATAR`, renomeia, escreve a fala, `Gerar` → **`Enviar`** —
  `/home/nmaldaner/projetos/inemaccbot/src/fila/tarefas/heygen-estudio.ts`,
  `/home/nmaldaner/projetos/inemaccbot/scripts/heygen-estudio.mjs`
- [confirmed] `motor_repo`, `templates_dir`, `template` no `FlowDef` — os dois
  últimos eram **descartados** por `congelar()` —
  `/home/nmaldaner/projetos/inemaccbot/src/dominio/flow.ts`
- [confirmed] Prazo do `baixar`: 90 min → **40h**, poll 2 → 5 min. A fila do
  HeyGen chega a 36h e **esperar não é erro**; o teto existe para o vídeo que
  nunca foi gerado.
- [confirmed] `playwright@1.57.0` fixado (casa com o Chromium já em
  `~/.cache/ms-playwright`); perfil logado em
  `~/.cache/inemaccbot/perfil-heygen` (`HEYGEN_PERFIL_CHROME`)
- [confirmed] **promoavatar3 autônomo**: `scripts/`, `templates/`,
  `.claude/skills/reel-edita-inema/` próprios; reel como função; regra 11b
  (`## IMAGENS` com `headline:`/`hook:`) no prompt da fase 1; HELP/README/
  CLAUDE.md atualizados
- [confirmed] Commits pushados: `inemaccbot` `c4aae17` `2363046` `e293f9c`
  `9509d2b` · `promoavatar` `bb0a83c` `d9e2064` · `promoavatar3` `18762b6`
  `d43e01e` `fda1c72`
- [confirmed] 791 testes, tsc limpo, build ok
- [confirmed] Memória atualizada em
  `/home/nmaldaner/.claude/projects/-home-nmaldaner-projetos-inemaccbot/memory/`

### Números medidos

| | antes | depois |
|---|---|---|
| custo por vídeo | US$ 3,09 | **US$ 0,18–0,50** |
| avatar por público | 313 s (agente) | **~50 s** (script), 84 avatares, 0 falha |
| reel por público | 220 s | 165 s, 96 reels, 0 falha de pipeline |
| fases com modelo | 3 | **1** (só o texto) |

## Proposed / attempted but not confirmed

- [unverified] **Nenhum reel do promoavatar3 rodou** — o caminho só se prova com
  um fluxo `C#` cujo texto tenha `## IMAGENS` (C13–C15 não têm)
- [unverified] O hífen (`pessoa-comum.md`) parou de aparecer no A#35 — 1 caso,
  pode ser acaso; nos A#32/33/34 apareceu e foi corrigido à mão
- [?] O 403 do Groq (A#35/empreendedores) foi transitório — causa desconhecida,
  o corpo da resposta HTTP não é guardado
- [not-wired] Não feitos: `iniciado_em`/`tentativas` no `claim`; validação dos
  nomes de arquivo na fase de texto; teste de integração que siga um reel até o
  canal; mensagem do `preparar.py` distinguir "faltou transcript" de "faltou
  IMAGENS" (só no promoavatar3)

## Key files for next session

- `/home/nmaldaner/projetos/inemaccbot/docs/custo-por-fase-a19-a29.md` — a
  medição e o método; **leia a "Ressalva de método sobre TEMPO" antes de medir**
- `/home/nmaldaner/projetos/inemaccbot/src/fila/tarefas/reel.ts` — os três
  defeitos da migração estão comentados no código
- `/home/nmaldaner/projetos/inemaccbot/scripts/heygen-estudio.mjs` — os quatro
  achados do HeyGen, incluindo o botão `Enviar`
- `/home/nmaldaner/projetos/promoavatar3/CLAUDE.md` — por que o motor foi
  copiado e não compartilhado
- `/home/nmaldaner/projetos/inemaccbot/src/integracao/promoavatar3.test.ts` —
  proíbe o reel de apontar para `projetos/promoavatar/`
- Plan file: none

## Running state (no fim da sessão)

- **15 fluxos rodando** (A#38 a A#52); A#30–A#37 completos (96 reels entregues)
- Filas: `navegador` 62 · `io` 46 · `texto` 5 · `render` 3
- **Zero falhas na última hora**
- Serviço `inemaccbot` ativo (systemctl --user), último restart com o binário de
  `e293f9c`; `http.server :8202` servindo `~/projetos/output`
- Monitor `by6h3zp2w` — **morre com o `/clear`**; para retomar a vigilância,
  peça ao próximo agente que arme outro
- Branches: tudo em `master`, tudo pushado
- Scratchpad dos scripts de análise (volátil):
  `/tmp/claude-1000/-home-nmaldaner-projetos-inemaccbot/d4e8f0cb-140b-41cf-8308-ec45de1cfcc2/scratchpad/`
  (`indexa.py` → `casa.py` → `tabela.py`)

## Verification

- `cd /home/nmaldaner/projetos/inemaccbot && npm test` — 791 passam
- `sqlite3 inemaccbot.db "select count(*) from jobs where status in ('running','queued');"`
  — **antes de qualquer restart**
- `find ~/projetos/yt-pub-lives*/ -name 'A3*-*.mp4'` — entrega no canal; procure
  no repo **inteiro**, não só em `imports/videos` (o pipeline do canal move para
  `lives/*/clips/`)
- `journalctl --user -u inemaccbot --since '-10 min' | grep 'entregue ao canal'`
- `node -e "const{carregarFlow}=require('./dist/dominio/flow.js');console.log(carregarFlow(process.env.HOME+'/projetos/promoavatar3',require('./config/skills.json').map(s=>s.command)).motor_repo)"`
  — deve imprimir `undefined`

## Deferred + open questions

- Deferred: renomear os 36 alvos do promoavatar3 (hífen) — inofensivo agora que
  ninguém parseia nome de arquivo
- Deferred: extrair o motor do reel para um repo próprio
- **Open: disco em 91%, 341 GB livres** — `~/projetos/output/reels` já tem 67 GB
  e há ~150 reels no funil (~280 MB de workspace cada).
  `/limpar artefatos 14` é dry-run por padrão
- Open: opus × sonnet na fase de texto — 4 fluxos de cada lado para comparar;
  opus custa ~US$ 2,8–5,9 por fluxo contra ~US$ 0,9

## Pick up here

Acompanhar os 15 fluxos até fecharem (o gargalo é a fila `render`,
concorrência 1). **A se o volume seguir**: vigiar o disco. **B se quiser fechar
dívida técnica**: `iniciado_em` no `claim` é o conserto de maior alcance —
enganou a medição duas vezes e pode matar um render por um prazo que ele nunca
gastou.
